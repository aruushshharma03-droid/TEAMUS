# Supabase (optional — demo runs without these)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Claude (optional — clustering has a deterministic fallback)
ANTHROPIC_API_KEY=

# Cron protection
CRON_SECRET=dev-cron

# Allow /api/dev/reset in production previews
ALLOW_DEMO_RESET=1
